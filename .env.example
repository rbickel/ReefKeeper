# Environment Variables for ReefKeeper

AUTH0_DOMAIN=***.eu.auth0.com
AUTH0_CLIENT_ID=***
AUTH0_CLIENT_SECRET=J-***
AUTH0_CALLBACK_URL=http://***:8081/authorize


# Test Credentials (used by Playwright E2E tests)
# Replace these placeholder values with actual test user credentials
# These should be credentials for a dedicated test user, not a production account
# In CI/CD, set these as secrets instead of using this file
TEST_USER_EMAIL=your-test-user@example.com
TEST_USER_PASSWORD=your-secure-test-password

# When authentication is added, uncomment and configure:
# AUTH_API_URL=http://localhost:3000
# AUTH_TOKEN_KEY=@reef_keeper:auth_token

AUTH0_CLIENT_ID_APK=***